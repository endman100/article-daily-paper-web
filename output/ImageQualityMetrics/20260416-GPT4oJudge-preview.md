<!-- ⚠️ 此檔案由程式自動產生，請勿直接修改。 如需更新內容，請修改來源 JSON 後重新執行 to_preview_md.py。 -->

如果要找一個最接近人類評審的自動化評估方案，直接讓最強的 MLLM 來當裁判或許是最直接的做法

GPT-4o 擁有極強的視覺理解和語言推理能力，把它當成 T2I 模型的自動化評審在技術上已經完全可行

這種做法跳過了所有中間環節——不需要計算嵌入向量、不需要估計分布距離、不需要拆解問題

直接把 prompt 和圖片交給 GPT-4o，讓它像人類一樣觀察並給出評價

這種基於 MLLM 的評審方式在近期的 T2I 研究中出現的頻率越來越高

它正在改變我們對自動化評估的基本認知框架 👇

----
使用 GPT-4o 作為評審的流程非常直觀

把文字 prompt 和生成的圖片一起傳送給 GPT-4o，搭配一段精心設計的評估指令

評估指令會明確告訴 GPT-4o 需要從哪些維度進行打分，以及每個分數等級的定義

GPT-4o 會根據這些指令輸出一個結構化的評估結果，包含分數和詳細的評語

有些實作會要求 GPT-4o 先進行逐步推理再給出最終分數，這種 CoT 方式通常能提升評估品質

整個流程的設計核心在於評估指令的撰寫，不同的指令會導致不同的評估標準

----
GPT-4o 作為評審最大的優勢在於它的理解深度和推理能力

它能處理非常複雜的 prompt，理解隱含的語意要求和風格暗示

生成的評語具有高度的可解釋性，研究者可以直接從文字中理解打分的邏輯

不需要為每種評估場景設計專門的指標或模型，只要調整評估指令就能適應不同的需求

這種靈活性讓它在面對新類型的生成任務時幾乎不需要額外的適配工作

在多項人類一致性測試中，GPT-4o 的評估結果跟人類評審的吻合度非常高

----
使用 GPT-4o 作為評審面臨幾個實務上的挑戰

API 呼叫的成本遠高於傳統指標的計算成本，大規模評估的費用相當可觀

每次呼叫都有延遲，在需要評估大量圖片的場景中速度會成為瓶頸

GPT-4o 的輸出具有隨機性，同一張圖片在不同次呼叫中可能得到不同的分數

這需要多次採樣取平均來提升穩定性，進一步增加了成本

評估指令的設計品質對結果有巨大的影響，微小的指令措辭變動可能導致評分標準的偏移

----
GPT-4o judge 代表了 T2I 評估從專用指標走向通用 AI 評審的趨勢

它跟 VIEScore 一樣屬於 MLLM 驅動的評估方式，但直接使用了目前最強大的商用模型

在研究的探索階段，它的靈活性和理解深度讓它成為非常有價值的評估工具

但在需要大規模自動化評估的生產環境中，成本和速度的限制使它不太適合作為唯一的指標

更常見的做法是用 GPT-4o judge 作為高階的品質校驗工具，搭配傳統指標做日常的快速評估

蒸餾技術讓研究者能把 GPT-4o 的評估能力壓縮到更小的模型中，降低成本的同時保留判斷品質

----