<!-- ⚠️ 此檔案由程式自動產生，請勿直接修改。 如需更新內容，請修改來源 JSON 後重新執行 to_preview_md.py。 -->

Transformer 架構在 IQA 任務中展現了卓越性能，但其 O(n²) 計算複雜度在處理高解析度圖像時代價高昂Mamba 架構（State Space Model，SSM）以線性計算複雜度處理長序列，成為 Transformer 的有力替代IQA 任務需要捕捉圖像的長範圍空間依賴關係（如整體一致性失真），SSM 的長序列建模優勢在此尤為突出Q-Mamba 是首個將 Mamba 架構應用於 NR-IQA 任務的工作，探索 SSM 在品質感知中的潛力核心貢獻：設計了品質感知的掃描策略，讓 Mamba 的狀態空間序列化能夠優先捕捉品質相關的圖像信息這篇 2024 年工作開創了「Mamba for IQA」的新研究方向，為高效高性能 IQA 提供了新思路 👇----Q-Mamba 的架構設計：視覺 Mamba 骨幹 + 品質感知掃描模組 + 品質回歸頭視覺 Mamba 骨幹：基於 VMamba 架構，將圖像塊序列輸入 SSM 進行特徵提取品質感知掃描策略：設計了「品質優先掃描」（Quality-Aware Scan）順序，優先處理失真嚴重區域品質優先掃描的實現：使用預訓練的輕量失真偵測器預先估計各圖像塊的失真程度，按失真程度排序掃描多尺度 SSM 特徵：在不同空間解析度的特徵圖上應用 SSM，捕捉多尺度的品質信息品質回歸頭：將全局 SSM 特徵池化後輸入 MLP，直接輸出歸一化品質分數----Q-Mamba 的關鍵技術創新：「品質狀態初始化」（Quality State Init）策略傳統 Mamba 從零初始化隱藏狀態，Q-Mamba 使用圖像的全局統計特徵（亮度、對比度）初始化狀態這種初始化策略讓 SSM 從一開始就具有對圖像整體品質的先驗知識，加速收斂並提升精度「品質感知 SSM 參數」：對 Mamba 的狀態轉移矩陣進行品質感知的輸入調製，增強對失真的敏感性效率設計：Q-Mamba 的計算量比同精度的 Transformer 方法低 2.8 倍，記憶體佔用低 3.1 倍訓練策略：先在大型圖像分類資料集上預訓練視覺 Mamba 骨幹，再在 IQA 資料集上微調----在 LIVE 資料集上，Q-Mamba 的 SRCC 達到 0.972，PLCC 達到 0.975在 KonIQ-10k 真實失真資料集上，SRCC 達到 0.933，與 Transformer 方法相當但速度快 2.8 倍在 SPAQ 手機攝影資料集上，Q-Mamba 達到 SRCC=0.919，展現了對多樣化真實失真的適應性效率對比：在 4K 解析度圖像上，Q-Mamba 比 IQT（Transformer-based）快 4.1 倍品質感知掃描的消融：使用品質優先掃描比隨機掃描的 SRCC 提升 0.8%，驗證了設計有效性跨資料集泛化：在 LIVE 訓練後直接在 CSIQ 測試，SRCC 比對比方法高 3.2%----Q-Mamba 開拓了 Mamba 架構在 IQA 任務中應用的新方向，受到廣泛關注品質感知掃描策略為其他需要優先處理特定區域的視覺理解任務提供了設計參考Q-Mamba 的高效率特性使其特別適合部署在計算資源受限的端側設備（手機、相機）相比 TOPIQ 等最新 Transformer IQA 方法，Q-Mamba 在效率-性能曲線上的表現更優Q-Mamba 的成功表明，Mamba 架構的長序列建模能力確實適合 IQA 的全圖品質理解需求為後續 Mamba-based IQA 工作奠定了基礎，品質感知掃描策略已被多個後續工作採用----

Q-Mamba 在 IQA 效率-性能平衡上達到了新的里程碑

未來應探索更智慧的掃描策略，如基於視覺顯著性或失真類型自適應調整掃描順序

Q-Mamba 的框架可擴展到視頻 IQA，利用 SSM 的時序建模能力處理視頻的時序品質變化

在端側 IQA 部署場景中，探索 Q-Mamba 與量化（quantization）和剪枝（pruning）的結合

研究 Mamba 的狀態空間如何更好地表示不同失真類型的特徵，進一步提升失真感知能力

Q-Mamba 預示著 SSM 架構將在 IQA 和其他視覺品質任務中佔有重要一席之地

----
