<!-- ⚠️ 此檔案由程式自動產生，請勿直接修改。 如需更新內容，請修改來源 JSON 後重新執行 to_preview_md.py。 -->

T2I 模型的評估長期在自動化（快速但精度有限）與人工評估（精確但成本高）之間面臨權衡

基於規則的自動指標（FID、CLIPScore）無法理解複雜的語意推理和常識性評估

大型多模態語言模型的出現提供了第三條路：讓 AI 模型扮演具有理解能力的智能評判員

GPT-4o 在視覺理解、語言推理和知識廣度上均達到了接近人類專家的水準

以 GPT-4o 作為 T2I 評估裁判，可以進行需要複雜推理的細粒度評估，不局限於預設的評估維度

這種 LLM-as-Judge 的評估範式代表了 T2I 評估從規則驅動向智能推理的重要轉型 👇

----
GPT-4o 評估流程：向 GPT-4o 提供生成圖片和文字 prompt，並配以詳細的評估指令

評估指令（System Prompt）精確定義評估標準：語意一致性、物件完整性、屬性準確性、空間關係

GPT-4o 輸出結構化的評估結果：每個評估維度的分數（通常 1-5 分）加上評分依據的文字說明

可以進行絕對評分（針對單張圖片）或相對比較（成對比較兩個生成結果）

Chain-of-Thought 提示使 GPT-4o 先分析圖片內容再給出評分，提升了評估的一致性

對於成對比較模式，GPT-4o 同時輸入兩張生成圖片和同一 prompt，判斷哪個更好及原因

----
評估指令設計是 GPT-4o Judge 可靠性的關鍵：模糊的指令導致高度不一致的評分

研究者開發了多種評估提示模板：全面評分提示、特定維度評分提示、成對比較提示

位置偏見（Position Bias）是 GPT-4o Judge 的已知問題：在成對比較中，左圖或右圖可能獲得系統性偏好

透過隨機化圖片順序並取雙向比較的平均分，可以有效消除位置偏見

為提高評估的穩定性，通常對同一評估任務進行多次採樣並取眾數或平均值

溫度（Temperature）設定影響評分的一致性：較低溫度（0.2-0.5）的評分重現性更高

----
在多個 T2I 評估基準的相關性研究中，GPT-4o Judge 與人類評分的 Spearman 相關係數超過 0.85

在需要常識推理的評估任務（如 WISE benchmark）中，GPT-4o 的準確率顯著優於所有傳統指標

相比 CLIPScore，GPT-4o Judge 在複雜組合語意（否定、計數、空間關係）上的評估準確率高出約 30%

在 DrawBench 和 PartiPrompts 的人類偏好預測任務上，GPT-4o Judge 達到了接近人類標注者的一致性

GPT-4o 能夠識別 T2I 模型生成的常識性錯誤（如太空燃燒的蠟燭形狀），這是傳統指標無法做到的

成對比較模式下，GPT-4o Judge 與人類偏好的 Elo 排名一致性達到了 90% 以上

----
相比 CLIPScore 和 FID，GPT-4o Judge 能處理開放式評估任務，不受固定評估維度限制

與蒸餾的 MLLM 裁判相比，GPT-4o Judge 提供了更高的評估天花板，但成本也顯著更高

相比人工評估，GPT-4o Judge 的速度快數百倍，同時在多個評估任務上達到人類水準的一致性

GPT-4o Judge 的主要局限是 API 呼叫成本：大規模評估（10 萬張圖片）的費用可能達到數千美元

GPT-4o Judge 的評估行為受提示設計影響顯著，不同研究間的可比性依賴標準化的提示模板

GPT-4o 的知識截止日期限制了其對最新事件和新發布模型生成結果的評估能力

----
GPT-4o 作為 T2I 評估裁判的實踐推動了 LLM-as-Judge 範式在視覺生成領域的廣泛應用

WISE、VIEScore 等多個 T2I 評估基準採用 GPT-4o 作為自動評審工具

GPT-4o Judge 的高精度評估為蒸餾裁判（DistilledMLLMJudge）提供了高品質的教師信號

隨著 Claude 3.5、Gemini 1.5 等強大多模態模型的出現，LLM Judge 的評估質量持續提升

開源 MLLM 評判員的能力提升使得低成本高品質的 T2I 自動評估逐漸變得可行

GPT-4o Judge 框架的標準化正在推動 T2I 評估從封閉基準向開放式智能評估的範式演進

----